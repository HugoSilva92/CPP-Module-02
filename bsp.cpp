#include "Point.hpp"

bool bsp( Point const a, Point const b, Point const c, Point const point){
    // a b c are the vertices of our triagle;
    // point is the point to check;
    // returns true if its in there false otherwise;
}